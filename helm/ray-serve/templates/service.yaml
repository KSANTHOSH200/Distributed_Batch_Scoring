apiVersion: v1
kind: Service
metadata:
  name: ray-serve
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ray-serve
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000